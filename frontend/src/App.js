import React from 'react';

const App = () => {
    return (
        <div>
            <h2>React</h2>
            <button onClick={() => alert('React ボタンがクリックされました！')}>
                クリック
            </button>
        </div>
    );
};

export default App;
